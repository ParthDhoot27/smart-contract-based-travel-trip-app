# Copy this file to .env and set values as needed
# If MONGODB_URI is set, backend uses MongoDB Atlas via Mongoose.
# If not set, backend falls back to SQLite (better-sqlite3).
PORT=4000
# Use a MongoDB URI without committing credentials. Set this only in your local .env or deployment secrets.
# Example: mongodb+srv://<username>:<password>@<cluster-host>/<database>?retryWrites=true&w=majority
MONGODB_URI=